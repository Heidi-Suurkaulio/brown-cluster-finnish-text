[Brown Clustering with Finnish text data]
name = "bronwcluster"
version = "0.1.0"
description = "Trying out brown clustering with Finnish text data."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "brown-clustering>=0.1.6",
]
